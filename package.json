{
  "dependencies": {
    "pusher-js": "^5.0.0",
    "uglifyjs": "^2.4.11",
    "webpack": "^5.75.0"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  },
  "scripts": {
    "build": "npx webpack\nnpm run uglify",
    "uglify": "npx uglifyjs dist/index.js -c -m --keep-fnames --keep-classnames > dist/index.min.js"
  }
}
